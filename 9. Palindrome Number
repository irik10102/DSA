class Solution 
{
public:
    bool isPalindrome(int x) 
    {
        if(x<0)
            return false;
        long long int c=x;
        long long int rev=0;
        while(c!=0)
        {
            rev=rev*10 + c%10;
            c=c/10;
            
        }
       return (rev == x);
    }
};
